initSidebarItems({"fn":[["vector_add","Function that takes two vectors, each representing some atribtrary unsigned values (e.g. `[1,2,3] = 123u32`), and return a result representing the addition between the two *numbers*."],["vector_sub","Function that takes two vectors, each representing some arbitrary unsigned values (e.g. `[1,1,1] = 111u32`), and return a result representing the  subtraction between the two *numbers*. Returns a `Result` as some subtraction may result in an overflow, how the error is handled is up to the caller"]]});